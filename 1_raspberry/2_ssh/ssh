# start ssh
sudo service ssh start
# auto start ssh after boot
vi /etc/rc.local
    ## add command
    /etc/init.d/ssh start
# start ssh at startup
sudo update-rc.d ssh enable


# login raspberry which run on qemu using ssh
ssh pi@127.0.0.1 -p 5022
