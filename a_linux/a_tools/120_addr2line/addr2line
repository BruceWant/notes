# convert address into file names and line numbers
addr2line <address> -e <executable_name> -f

# convert offset to function name, file name and line numbers
addr2line -C -f -e -i <binary_file> offset


