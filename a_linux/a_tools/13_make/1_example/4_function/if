# command: make -f if [var1=<argument>]
# syntax: $(if condition,then-part[,else-part])
var:=asd
ord:=$(if $(var), $(if $(var1), st, else), outside)
all:
	@echo $(if string_is_nonempty, true, false)
	@echo $(ord)
