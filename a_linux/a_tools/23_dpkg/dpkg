# list packages matching giving pattern
dpkg -l
# e.g. list binutils's version
dpkg -l | grep binutils

# extract packge
dpkg -x ./<package_name>.deb ./<destination_folder>
dpkg -e ./<package_name>.deb ./<destination_folder>/DEBIAN

# find which files were installed by a package
dpkg -L <package>

